Congratulations! <%= @buyer.company.present? ? @buyer.company : "#{@buyer.firstname} #{@buyer.lastname}" %> has invited to participate in a PromoExchange auction.

Accepting this invitation gives you a leg up on the competition because your PX fee is only 2.99% for this auction.


<% if @type == 'non' %>
  <%= Spree::Store.first.url %>/pxusers/new?auction_ref=<%= Spree::Store.first.url %>/auctions/<%= @auction.id %>">
<% else %>
  <%= Spree::Store.first.url %>/auctions/<%= @auction.id %>
<% end %>

Good luck!
The PromoExchange Team
